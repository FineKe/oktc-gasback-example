# Gasback on OKTC example
the example will guide you how to register gasback on oktc
the exmaple uses OKTC testnet by default.

To register gasback for your contract, you have to change  `PrivKey`、 `contractAddress` variables and find the nonce of the tx that you have deployed first time